# Alpha Vantage API Configuration
# Get your free API key at: https://www.alphavantage.co/support/#api-key
#
# FREE TIER LIMITS (2025):
# - 25 API calls per day (NOT 500!)
# - 5 API calls per minute
# - End-of-day data only (no intraday updates)
# - Free endpoints: TIME_SERIES_DAILY, GLOBAL_QUOTE, HISTORICAL_OPTIONS (includes Greeks!)
# - Premium only: REALTIME_OPTIONS, TIME_SERIES_DAILY_ADJUSTED
#
# IMPORTANT: Each symbol uses 2 API calls (stock + options), so max ~12 symbols per day
ALPHA_VANTAGE_API_KEY=your_api_key_here

# Frontend URL for CORS
FRONTEND_URL=http://localhost:3000
